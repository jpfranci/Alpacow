{
  "scripts": {
    "frontend:prettier": "cd client && yarn run prettier"
  },
  "devDependencies": {
    "husky": "^6.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run frontend:prettier"
    }
  }
}
