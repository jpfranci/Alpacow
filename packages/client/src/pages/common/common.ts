// module for common styled components we might encounter
import styled from "styled-components";

// hr tags inside flex parent els need this to display properly
export const StyledHR = styled.hr`
  margin-left: 0;
  margin-right: 0;
`;
